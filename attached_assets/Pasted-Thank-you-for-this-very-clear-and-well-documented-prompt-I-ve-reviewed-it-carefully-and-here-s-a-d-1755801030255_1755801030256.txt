Thank you for this very clear and well-documented prompt. I’ve reviewed it carefully, and here’s a detailed diagnosis and resolution path.

🧨 Core Issue
Yes, you are using the wrong filter parameter names for the RescueGroups API v5. This is why all your filters are being ignored and you're getting the same 20 default dogs for every query.

✅ Correct Parameter Names for RescueGroups v5 Filters
Here's how RescueGroups API v5 handles filtering:

The filters must use the actual attribute names as defined in the schema, not friendly names like sizeGroup or ageGroup.

🔍 Working Attributes:
According to the official RescueGroups API v5 docs, here are the correct filter keys:

Feature	✅ Correct Filter Key
Size	filter[animalSizes] (not sizeGroup)
Age	filter[animalGeneralAge] (not ageGroup)
Special Needs	filter[animalSpecialneeds] (not specialNeeds)
Species	filter[animalSpecies]
Status	filter[animalStatus]

🔧 Example Fixes
❌ Wrong:
js
Copy
Edit
params.append('filter[sizeGroup]', 'Large');
params.append('filter[ageGroup]', 'Senior');
params.append('filter[specialNeeds]', 'true');
✅ Correct:
js
Copy
Edit
params.append('filter[animalSizes]', 'Large');
params.append('filter[animalGeneralAge]', 'Senior');
params.append('filter[animalSpecialneeds]', 'true');
🧪 Updated Full Example Query (GET)
http
Copy
Edit
GET https://api.rescuegroups.org/v5/public/animals/search/available/dogs?filter[animalStatus]=Available&filter[animalSpecies]=Dog&filter[animalGeneralAge]=Senior&filter[animalSpecialneeds]=true&filter[animalSizes]=Large&limit=20&sort=random
🧠 Additional Notes
filter[updated] should use ISO date format: filter[updated]=>2025-01-01

If you use invalid or misspelled keys, RescueGroups does not return an error, it just ignores the filter.

sizeGroup, specialNeeds, and ageGroup are not real schema fields. They're developer-friendly labels from their web UI, not API field names.

✅ Action Plan for You
Replace all filter[...] keys with official schema field names from:

https://userguide.rescuegroups.org/display/APIDG/Animals+Public+Search+Fields

Retest with:

A unique value for each filter (e.g. animalGeneralAge=Senior)

Randomized sort

Logging actual filters in shell output

Check that dogs returned match the filters (e.g., no young dogs for Senior filter)

🟢 Final Verdict
Yes — your filters were being silently ignored due to incorrect parameter names like sizeGroup, specialNeeds, and ageGroup.

Replace them with the real field names like animalSizes, animalSpecialneeds, and animalGeneralAge and everything will work as expected.